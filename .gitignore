# Claude Code
.claude/

# Plugin config files (may contain sensitive data)
*_config.json

# Python cache
__pycache__/
*.pyc
*.pyo

# VPS deployment (local use only)
.vps/
.vps_config.json

# Temporary files
nul
*.exe

# VPS upload directory (not needed in git)
.vps_uploaded/

# Debug plugins directory (local use only - contains plugin copies)
debug/

# Documentation (local use only)
docs/

# Old plugins directory (migrated to new structure)
plugins/

# VPS uploaded plugins directory (not needed in git)
.vps_uploaded/

# Local plugins in development (not ready for git)
userinfo/
